import React from "react";

// The name here doesn't matter, as long as the file name matches
const LoadingComponent = () => {
  return <div>LoadingComponent</div>;
};

export default LoadingComponent;
